{
  "name": "livingshop",
  "private": true,
  "version": "1.0.0",
  "description": "Root project — hosts the git hooks and formatting config shared by apps/api and apps/web.",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "prettier": "^3.4.2"
  },
  "lint-staged": {
    "*.{ts,tsx,js,json,md}": [
      "prettier --write"
    ]
  }
}
